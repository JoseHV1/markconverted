services:
  back:
    build:
      context: ./back
    environment:
      - PORT=3010
      - CORS_ORIGIN=*
    ports:
      - "4111:3010"
    restart: unless-stopped

  front:
    build:
      context: ./front
    ports:
      - "4112:80"
    restart: unless-stopped
